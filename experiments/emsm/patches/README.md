# Patch register

No patch was applied during Phase A.

Any future compiler, build-system, or API-only patch must be stored here as an
independent reversible diff with a statement that it does not change the EMSM
algorithm, sampler, parameters, or security checks.

