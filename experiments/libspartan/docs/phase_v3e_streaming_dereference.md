# Phase V3E Streaming Dereference

FS6 extends FS5 with direct scalar-iterator commitment, bounded per-table
dereference regeneration, source-fused openings, and streaming query weights.
It never collects the complete dereference vector. The existing verifier and
all transcript labels/order remain unchanged.

The authoritative measurements and classifications are in
`../../v3e/v3e_results.json` and `../../../docs/v3e_dereference_peak_audit.md`.
