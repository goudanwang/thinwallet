# Resume Plan: Physical Android Device

This plan is inactive until V4E reaches PASS and an authorized ARM64 Android
device is available. Preserve desktop and ARM64 cross-build artifacts, verify
device ownership/authorization, record hardware/OS/thermal state, deploy the
headless benchmark, run correctness before caps, then run the approved boundary
matrix. Do not infer device feasibility from desktop or cross-compilation.
